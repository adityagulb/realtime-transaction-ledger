-- SnowSQL / Snowflake CLI friendly wrapper.
-- The storage integration step requires an AWS IAM trust-policy update before stage creation.
-- For that reason, this wrapper is intentionally split around the storage integration.

-- Execute manually in order using the README.
